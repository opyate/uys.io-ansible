---
- name: Nginx | Copy static content
  copy: src=uys.io.tgz dest={{stuff}}/uys.io.tgz
  sudo: yes

- name: Nginx | Extract static content
  command: creates={{stuff}}/uys.io /bin/tar -xzf {{stuff}}/uys.io.tgz -C {{stuff}}
  sudo: yes


